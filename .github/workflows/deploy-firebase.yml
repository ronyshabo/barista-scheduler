name: Deploy Firebase Hosting

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'firebase.json'
      - '.firebaserc'
      - '.github/workflows/deploy-firebase.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Firebase Tools
        run: npm install -g firebase-tools

      - name: Firebase Deploy
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
        run: |
          if [ -z "$FIREBASE_TOKEN" ]; then
            echo "FIREBASE_TOKEN secret is missing. Create it with 'firebase login:ci' output." >&2
            exit 1
          fi
          if [ -z "$FIREBASE_PROJECT_ID" ]; then
            echo "FIREBASE_PROJECT_ID secret is missing (your Firebase project ID)." >&2
            exit 1
          fi
          firebase deploy --only hosting --project "$FIREBASE_PROJECT_ID" --non-interactive
